# CLevel02AGame

## Identity

| Item | Value |
|---|---|
| RTTI name | `CLevel02AGame` |
| Base chain | `CJimmyGame -> CGameType -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004c5bfc, 004c5c0c, 004c605c, 004c6070` |
| Ctor(s) | inherits the `CJimmyGame` construction path; installs the `CLevel02AGame` vftables and binds this level's class id |
| Dtor(s) | scalar deleting destructor `vfunc_02_002` at `00450a40` (destroys the `OMediaClassStreamer` at `this+0x163`) |
| Ledger row | `docs/decomp_ledger.csv` |

`CLevel02AGame` is the **Level 2A game-mode controller** — a leaf subclass of [`CJimmyGame`](./CJimmyGame.md). It walks 374 vtable slots with **1 owned method** (a per-class destructor only), so its entire game lifecycle (InitGame/UnInitGame/update, the active player tick, mission timers, `SoundEffects.omt`) is inherited from `CJimmyGame` / [`CGameType`](./CGameType.md). What distinguishes it is the level it binds: **`level2a.gam`**.

## Level Data (`level2a.gam`)

| Item | Value |
|---|---|
| `.gam` file | `level2a.gam` |
| Placed objects | 178 |
| Distinct object FourCC types | 25 |

Object FourCC composition (top types):

| FourCC | Count |
|---|---:|
| `3PAT` | 31 |
| `3PIC` | 27 |
| `3SOU` | 16 |
| `3NEU` | 15 |
| `3CHK` | 10 |
| `3SPR` | 9 |
| `3AIT` | 8 |
| `3TRE` | 8 |
| `STRT` | 7 |
| `3ARR` | 6 |
| `3OMT` | 6 |
| `LOAD` | 6 |

Full per-object placement, properties, and tuning for this level live in `docs/gam_schema.md` (cross-level FourCC schema) and `docs/tsk_data/all_entities.json` (this level's survey). The controller itself does not re-implement object behavior — it loads the `.gam` and lets each placed object's class run.

## Vtable Methods

| Slot | Address | Name | Behavior | Status |
|---|---|---|---|---|
| `vfunc_02_002` | `00450a40` | Scalar | scalar deleting destructor | trivial |

## Confidence

Confidence: Medium-Low

Validation: Ghidra `DumpClass.java CLevel02AGame` (slots=374, owned_methods=1); vftables/base chain from `docs/decomp_ledger.csv`; level object survey from `tools/build_level_entity_index.py`. Behavior is inherited from `CJimmyGame` (separately specced and the substantive parent). Not runtime-validated.

Open questions:
- Pin this class's constructor and the exact level class-id immediate it registers.
- Confirm the `.gam`/`.tsk` binding is established in the ctor vs. an inherited load slot.

## Notes

- Generated by `tools/gen_level_game_specs.py` from the Ghidra dump + `all_entities.json`. Leaf of `CJimmyGame`; see that spec for the shared controller behavior.
