# CLevel05BGame

## Identity

| Item | Value |
|---|---|
| RTTI name | `CLevel05BGame` |
| Base chain | `CJimmyGame -> CGameType -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004cb51c, 004cb52c, 004cb97c, 004cb990` |
| Ctor(s) | inherits the `CJimmyGame` construction path; installs the `CLevel05BGame` vftables and binds this level's class id |
| Dtor(s) | scalar deleting destructor `vfunc_02_002` at `00454c70` (destroys the `OMediaClassStreamer` at `this+0x163`) |
| Ledger row | `docs/decomp_ledger.csv` |

`CLevel05BGame` is the **Level 5B game-mode controller** — a leaf subclass of [`CJimmyGame`](./CJimmyGame.md). It walks 374 vtable slots with **1 owned method** (a per-class destructor only), so its entire game lifecycle (InitGame/UnInitGame/update, the active player tick, mission timers, `SoundEffects.omt`) is inherited from `CJimmyGame` / [`CGameType`](./CGameType.md). What distinguishes it is the level it binds: **`Level5b.gam`**.

## Level Data (`Level5b.gam`)

| Item | Value |
|---|---|
| `.gam` file | `Level5b.gam` |
| Placed objects | 259 |
| Distinct object FourCC types | 21 |

Object FourCC composition (top types):

| FourCC | Count |
|---|---:|
| `3ROK` | 99 |
| `3PAT` | 48 |
| `3PIC` | 23 |
| `3NEU` | 20 |
| `3AIT` | 16 |
| `3GEY` | 9 |
| `3TUR` | 9 |
| `3CAM` | 8 |
| `3RED` | 8 |
| `3STA` | 6 |
| `STRT` | 3 |
| `3CAR` | 1 |

Full per-object placement, properties, and tuning for this level live in `docs/gam_schema.md` (cross-level FourCC schema) and `docs/tsk_data/all_entities.json` (this level's survey). The controller itself does not re-implement object behavior — it loads the `.gam` and lets each placed object's class run.

## Vtable Methods

| Slot | Address | Name | Behavior | Status |
|---|---|---|---|---|
| `vfunc_02_002` | `00454c70` | Scalar | scalar deleting destructor | trivial |

## Confidence

Confidence: Medium-Low

Validation: Ghidra `DumpClass.java CLevel05BGame` (slots=374, owned_methods=1); vftables/base chain from `docs/decomp_ledger.csv`; level object survey from `tools/build_level_entity_index.py`. Behavior is inherited from `CJimmyGame` (separately specced and the substantive parent). Not runtime-validated.

Open questions:
- Pin this class's constructor and the exact level class-id immediate it registers.
- Confirm the `.gam`/`.tsk` binding is established in the ctor vs. an inherited load slot.

## Notes

- Generated by `tools/gen_level_game_specs.py` from the Ghidra dump + `all_entities.json`. Leaf of `CJimmyGame`; see that spec for the shared controller behavior.
