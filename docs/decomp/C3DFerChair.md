# C3DFerChair

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DFerChair` |
| Base chain | `C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `0049b20c, 0049b21c, 0049b66c, 0049b6a8, 0049b6bc` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DFerChair` is a placeable **mechanisms moving parts** object (family `mechanisms_moving_parts`, wave 6). It walks the class vtable with 1 owned method; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

No own `.gam` properties registered in `InitObject` (inherits its parent's property set, or is created at runtime rather than placed). See `docs/gam_schema.md` for any inherited properties.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `00418bd0` | InitObject (property + asset registration) | loads `chair.ase`, `wheel.png`, `DEFAULT` |

### Decompiled owned methods

**`vfunc_01_007` @ `00418bd0`** — InitObject (property + asset registration)

```c
void __thiscall C3DFerChair::vfunc_01_007(C3DFerChair *this)

{
  C3DFerChair *pCVar1;
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DAnimated::vfunc_01_007((C3DAnimated *)this);
  pCVar1 = this + -0x30;
  (**(code **)(this[-0x30].vftable + 0x108))();
  *(undefined1 *)((int)&this[0x15d].vftable + 1) = 1;
  (**(code **)(pCVar1->vftable + 0xd8))(s_HIDEFAULT_004ed8e4,s_chair_ase_004ee44c);
  (**(code **)(pCVar1->vftable + 0xf0))(s_wheel_png_004ee440,0);
  (**(code **)(pCVar1->vftable + 0xf4))(this[0x12f].vftable,0);
  (**(code **)(pCVar1->vftable + 0xe0))(s_DEFAULT_004ee39c,1);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

## Assets

| Kind | Name | Notes |
|---|---|---|
| ASE/anim | `chair.ase` | anim tag `HIDEFAULT` |
| PNG texture | `wheel.png` |  |
| default anim | `DEFAULT` | flag 1 |

## Confidence

Confidence: Low-Medium

Validation: Ghidra `DumpClass.java C3DFerChair` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
