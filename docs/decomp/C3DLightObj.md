# C3DLightObj

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DLightObj` |
| Base chain | `C3DSpriteType -> C3DSprite -> OMediaCanvasElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004a685c, 004a686c, 004a6cbc, 004a6cd0` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DLightObj` is a placeable **world props terrain** object (family `world_props_terrain`, wave 7). It walks the class vtable with 1 owned method; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

Offsets are from the primary class pointer; types are the `.gam` serialization type ids (`1=string 2=flag4 3=float 4=raw4 6=int`).

| Offset | Type | Property | Source |
|---:|---|---|---|
| `0x14b` | float | `Alpha` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x148` | float | `Red` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x149` | float | `Green` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x14a` | float | `Blue` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x14d` | int | `Activated` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x14c` | int | `Pulsate` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x14e` | float | `Period` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x151` | float | `OffsetTime` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x152` | int | `OnSoundIndex` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x153` | int | `OffSoundIndex` | `InitObject` registrar (`vftable+0x3fc`) |

See `docs/gam_schema.md` for the per-FourCC value ranges/samples across all 35 levels (the field map, constants, and object wiring are data-driven from there).

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `0042ddc0` | InitObject (property + asset registration) | registers 10 `.gam` properties (`Alpha`, `Red`, `Green`, `Blue`, `Activated`, `Pulsate`, `Period`, `OffsetTime`, `OnSoundIndex`, `OffSoundIndex`) |

### Decompiled owned methods

**`vfunc_01_007` @ `0042ddc0`** — InitObject (property + asset registration)

```c
void __thiscall C3DLightObj::vfunc_01_007(C3DLightObj *this)

{
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DSprite::vfunc_01_007((C3DSprite *)this);
  (**(code **)(this->vftable + 0x3fc))(s_Alpha_004ed560,this + 0x14b,3,0);
  (**(code **)(this->vftable + 0x3fc))(&PTR_DAT_004ed55c,this + 0x148,3,0);
  (**(code **)(this->vftable + 0x3fc))(s_Green_004ed554,this + 0x149,3,0);
  (**(code **)(this->vftable + 0x3fc))(&DAT_004ed54c,this + 0x14a,3,0);
  (**(code **)(this->vftable + 0x3fc))(s_Activated_004ed540,this + 0x14d,6,0);
  (**(code **)(this->vftable + 0x3fc))(s_Pulsate_004efb14,this + 0x14c,6,0);
  (**(code **)(this->vftable + 0x3fc))(s_Period_004efb0c,this + 0x14e,3,0);
  (**(code **)(this->vftable + 0x3fc))(s_OffsetTime_004efb00,this + 0x151,3,0);
  (**(code **)(this->vftable + 0x3fc))(s_OnSoundIndex_004ed530,this + 0x152,6,0);
  (**(code **)(this->vftable + 0x3fc))(s_OffSoundIndex_004ed520,this + 0x153,6,0);
  (**(code **)(this->vftable + 0x110))(0x41200000);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DLightObj` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
