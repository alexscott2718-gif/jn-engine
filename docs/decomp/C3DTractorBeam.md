# C3DTractorBeam

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DTractorBeam` |
| Base chain | `C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004bb628, 004bb638, 004bba88, 004bbac4, 004bbad8` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DTractorBeam` is a placeable **mechanisms moving parts** object (family `mechanisms_moving_parts`, wave 6). It walks the class vtable with 1 owned method; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

No own `.gam` properties registered in `InitObject` (inherits its parent's property set, or is created at runtime rather than placed). See `docs/gam_schema.md` for any inherited properties.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `00446470` | InitObject (property + asset registration) | loads `yrayupdown.ASE`, `yraystop.ASE`, `yray1.png`, `yray2.png`, `STOP` |

### Decompiled owned methods

**`vfunc_01_007` @ `00446470`** — InitObject (property + asset registration)

```c
void __thiscall C3DTractorBeam::vfunc_01_007(C3DTractorBeam *this)

{
  C3DTractorBeam *pCVar1;
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DAnimated::vfunc_01_007((C3DAnimated *)this);
  pCVar1 = this + -0x30;
  (**(code **)(this[-0x30].vftable + 0x108))();
  (**(code **)(pCVar1->vftable + 0xd8))(&DAT_004ed9c0,s_yrayupdown_ASE_004f129c);
  (**(code **)(pCVar1->vftable + 0xd8))(s_HISTOP_004ec9f4,s_yraystop_ASE_004f128c);
  (**(code **)(pCVar1->vftable + 0xf0))(s_yray1_png_004f1280,0);
  (**(code **)(pCVar1->vftable + 0xf0))(s_yray2_png_004f1274,1);
  (**(code **)(pCVar1->vftable + 0xf4))(this[0x12f].vftable,0);
  (**(code **)(this->vftable + 0x110))(0x41200000);
  (**(code **)(pCVar1->vftable + 0xe0))(&DAT_004ed040,1);
  (**(code **)(pCVar1->vftable + 0x114))(0x3f000000);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

## Assets

| Kind | Name | Notes |
|---|---|---|
| ASE/anim | `yrayupdown.ASE` | anim tag `HIUP` |
| ASE/anim | `yraystop.ASE` | anim tag `HISTOP` |
| PNG texture | `yray1.png` |  |
| PNG texture | `yray2.png` |  |
| default anim | `STOP` | flag 1 |

## Confidence

Confidence: Low-Medium

Validation: Ghidra `DumpClass.java C3DTractorBeam` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
