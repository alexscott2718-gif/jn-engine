# C3DVRTrophy

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DVRTrophy` |
| Base chain | `C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004be3f4, 004be404, 004be854, 004be890, 004be8a4` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DVRTrophy` is a placeable **pickups items** object (family `pickups_items`, wave 5). It walks the class vtable with 3 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

No own `.gam` properties registered in `InitObject` (inherits its parent's property set, or is created at runtime rather than placed). See `docs/gam_schema.md` for any inherited properties.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `00448e70` | InitObject (property + asset registration) | inherited init + class registration |
| `vfunc_01_010` | `00448ef0` | post-init / per-frame logic | runs inherited per-frame logic then this class's update step |
| `vfunc_04_067` | `00448e10` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_007` @ `00448e70`** — InitObject (property + asset registration)

```c
void __thiscall C3DVRTrophy::vfunc_01_007(C3DVRTrophy *this)

{
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DAnimated::vfunc_01_007((C3DAnimated *)this);
  (**(code **)(this[-0x30].vftable + 0x108))();
  (**(code **)(this->vftable + 0x110))(0x41a00000);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

**`vfunc_01_010` @ `00448ef0`** — post-init / per-frame logic

```c
void __thiscall C3DVRTrophy::vfunc_01_010(C3DVRTrophy *this)

{
  CLocalGameObject::vfunc_00_010((CLocalGameObject *)this);
  if (this[-0x14].vftable != (undefined *)0x0) {
    this[-0x14].vftable = (undefined *)0x1;
    (**(code **)(this[-0x30].vftable + 0x58))(0);
  }
  return;
}
```

**`vfunc_04_067` @ `00448e10`** — owned override

```c
void __thiscall C3DVRTrophy::vfunc_04_067(C3DVRTrophy *this)

{
  undefined *puVar1;
  
  if (*(char *)((int)&this[0x18d].vftable + 1) == '\0') {
    puVar1 = this->vftable;
    *(undefined1 *)((int)&this[0x18d].vftable + 1) = 1;
    (**(code **)(puVar1 + 0xd8))(s_HIDEFAULT_004ed8e4,s_trophy_ase_004f16e4);
    (**(code **)(this->vftable + 0xf0))(s_trophy_png_004f16d8,0);
    (**(code **)(this->vftable + 0xf4))(this[0x15f].vftable,0);
    (**(code **)(this->vftable + 0xe0))(s_DEFAULT_004ee39c,1);
  }
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DVRTrophy` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
