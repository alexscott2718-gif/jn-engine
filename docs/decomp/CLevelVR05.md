# CLevelVR05

## Identity

| Item | Value |
|---|---|
| RTTI name | `CLevelVR05` |
| Base chain | `CJimmyGame -> CGameType -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004cea8c, 004cea9c, 004ceeec, 004cef00` |
| Ctor(s) | inherits the `CJimmyGame` construction path; installs the `CLevelVR05` vftables and binds this level's class id |
| Dtor(s) | scalar deleting destructor `vfunc_02_002` at `00456de0` (destroys the `OMediaClassStreamer` at `this+0x163`) |
| Ledger row | `docs/decomp_ledger.csv` |

`CLevelVR05` is the **VR challenge level 05 game-mode controller** — a leaf subclass of [`CJimmyGame`](./CJimmyGame.md). It walks 374 vtable slots with **1 owned method** (a per-class destructor only), so its entire game lifecycle (InitGame/UnInitGame/update, the active player tick, mission timers, `SoundEffects.omt`) is inherited from `CJimmyGame` / [`CGameType`](./CGameType.md). What distinguishes it is the level it binds: **`VR05.gam`**.

## Level Data (`VR05.gam`)

| Item | Value |
|---|---|
| `.gam` file | `VR05.gam` |
| Placed objects | 18 |
| Distinct object FourCC types | 9 |

Object FourCC composition (top types):

| FourCC | Count |
|---|---:|
| `3PIC` | 9 |
| `3PAT` | 2 |
| `3JIM` | 1 |
| `3MCA` | 1 |
| `3PHO` | 1 |
| `3ROC` | 1 |
| `3TRO` | 1 |
| `LOAD` | 1 |
| `STRT` | 1 |

Full per-object placement, properties, and tuning for this level live in `docs/gam_schema.md` (cross-level FourCC schema) and `docs/tsk_data/all_entities.json` (this level's survey). The controller itself does not re-implement object behavior — it loads the `.gam` and lets each placed object's class run.

## Vtable Methods

| Slot | Address | Name | Behavior | Status |
|---|---|---|---|---|
| `vfunc_02_002` | `00456de0` | Scalar | scalar deleting destructor | trivial |

## Confidence

Confidence: Medium-Low

Validation: Ghidra `DumpClass.java CLevelVR05` (slots=374, owned_methods=1); vftables/base chain from `docs/decomp_ledger.csv`; level object survey from `tools/build_level_entity_index.py`. Behavior is inherited from `CJimmyGame` (separately specced and the substantive parent). Not runtime-validated.

Open questions:
- Pin this class's constructor and the exact level class-id immediate it registers.
- Confirm the `.gam`/`.tsk` binding is established in the ctor vs. an inherited load slot.

## Notes

- Generated by `tools/gen_level_game_specs.py` from the Ghidra dump + `all_entities.json`. Leaf of `CJimmyGame`; see that spec for the shared controller behavior.
