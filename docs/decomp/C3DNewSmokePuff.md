# C3DNewSmokePuff

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DNewSmokePuff` |
| FourCC | (not resolved; not a `.gam`-placed object or id unmapped) |
| Base chain | `C3DSpriteType -> C3DSprite -> OMediaCanvasElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004ab22c, 004ab23c, 004ab68c, 004ab6a0` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DNewSmokePuff` is a placeable **effects triggers nav cameras sound** object (family `effects_triggers_nav_cameras_sound`, wave 8). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

No own `.gam` properties registered in `InitObject` (inherits its parent's property set, or is created at runtime rather than placed). See `docs/gam_schema.md` for any inherited properties.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_259` | `004332b0` | owned override | see decompiled body |
| `vfunc_03_053` | `004332d0` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_259` @ `004332b0`** — owned override

```c
void __thiscall C3DNewSmokePuff::vfunc_01_259(C3DNewSmokePuff *this)

{
  C3DSprite::vfunc_01_259((C3DSprite *)this);
  this[-5].vftable = (undefined *)0x6;
  this[-4].vftable = (undefined *)0x1;
  return;
}
```

**`vfunc_03_053` @ `004332d0`** — owned override

Interpreted: fires an exit/deactivate action (slot `0x58`).

```c
void __thiscall C3DNewSmokePuff::vfunc_03_053(C3DNewSmokePuff *this)

{
  *(undefined1 *)&this[0x188].vftable = 1;
  this[0x17a].vftable = (undefined *)0x0;
  this[0x189].vftable = (undefined *)(float)(int)this[0x15f].vftable;
  (**(code **)(this[0x32].vftable + 0x314))();
  if (this[0x1c].vftable != (undefined *)0x0) {
    this[0x1c].vftable = (undefined *)0x1;
    (**(code **)(this->vftable + 0x58))(0);
  }
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Validation

No registered `.gam` properties to cross-check (inherited property set or runtime-created object).

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DNewSmokePuff` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
