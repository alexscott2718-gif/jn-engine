# C3DSteamVent

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DSteamVent` |
| Base chain | `C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004b7b98, 004b7ba8, 004b7ff8, 004b8034, 004b8048` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DSteamVent` is a placeable **mechanisms moving parts** object (family `mechanisms_moving_parts`, wave 6). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

Offsets are from the primary class pointer; types are the `.gam` serialization type ids (`1=string 2=flag4 3=float 4=raw4 6=int`).

| Offset | Type | Property | Source |
|---:|---|---|---|
| `0x17f` | float | `Red` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x180` | float | `Green` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x181` | float | `Blue` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x182` | int | `ButtonAvailable` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x183` | int | `NASound` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x184` | int | `AvailSound` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x185` | string | `NextButton` | `InitObject` registrar (`vftable+0x3fc`) |

See `docs/gam_schema.md` for the per-FourCC value ranges/samples across all 35 levels (the field map, constants, and object wiring are data-driven from there).

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `00442b70` | InitObject (property + asset registration) | registers 7 `.gam` properties (`Red`, `Green`, `Blue`, `ButtonAvailable`, `NASound`, `AvailSound`, `NextButton`); loads `buttondown.ase`, `buttonup.ase`, `switch.png`, `UP` |
| `vfunc_01_259` | `004432c0` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_007` @ `00442b70`** — InitObject (property + asset registration)

```c
void __thiscall C3DSteamVent::vfunc_01_007(C3DSteamVent *this)

{
  C3DSteamVent *pCVar1;
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DAnimated::vfunc_01_007((C3DAnimated *)this);
  (**(code **)(this->vftable + 0x3fc))(&PTR_DAT_004ed55c,this + 0x17f,3,0);
  (**(code **)(this->vftable + 0x3fc))(s_Green_004ed554,this + 0x180,3,0);
  (**(code **)(this->vftable + 0x3fc))(&DAT_004ed54c,this + 0x181,3,0);
  (**(code **)(this->vftable + 0x3fc))(s_ButtonAvailable_004eda2c,this + 0x182,6,0);
  (**(code **)(this->vftable + 0x3fc))(s_NASound_004eda24,this + 0x183,6,0);
  (**(code **)(this->vftable + 0x3fc))(s_AvailSound_004eda18,this + 0x184,6,0);
  (**(code **)(this->vftable + 0x3fc))(s_NextButton_004f0f5c,this + 0x185,1,0);
  pCVar1 = this + -0x30;
  (**(code **)(this[-0x30].vftable + 0x108))();
  (**(code **)(pCVar1->vftable + 0xd8))(s_HIDOWN_004ed9c8,s_buttondown_ase_004ed98c);
  (**(code **)(pCVar1->vftable + 0xd8))(&DAT_004ed9c0,s_buttonup_ase_004ed97c);
  (**(code **)(pCVar1->vftable + 0xf0))(s_switch_png_004ed948,0);
  (**(code **)(pCVar1->vftable + 0xf4))(this[0x12f].vftable,0);
  (**(code **)(this->vftable + 0x110))(0x43fa0000);
  (**(code **)(pCVar1->vftable + 0xe0))(&DAT_004ed9bc,1);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

**`vfunc_01_259` @ `004432c0`** — owned override

```c
void __thiscall C3DSteamVent::vfunc_01_259(C3DSteamVent *this)

{
  C3DAnimated::vfunc_01_259((C3DAnimated *)this);
  (**(code **)(this->vftable + 0x404))();
  (**(code **)(this->vftable + 0x110))(0x42c80000);
  return;
}
```

## Assets

| Kind | Name | Notes |
|---|---|---|
| ASE/anim | `buttondown.ase` | anim tag `HIDOWN` |
| ASE/anim | `buttonup.ase` | anim tag `HIUP` |
| PNG texture | `switch.png` |  |
| default anim | `UP` | flag 1 |

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DSteamVent` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
