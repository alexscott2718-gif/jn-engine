# C3DYokCargo

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DYokCargo` |
| Base chain | `C3DAI -> C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004befac, 004befbc, 004bf40c, 004bf448, 004bf45c` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DYokCargo` is a placeable **mechanisms moving parts** object (family `mechanisms_moving_parts`, wave 6). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

No own `.gam` properties registered in `InitObject` (inherits its parent's property set, or is created at runtime rather than placed). See `docs/gam_schema.md` for any inherited properties.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_010` | `004496c0` | owned override | see decompiled body |
| `vfunc_01_265` | `00449bb0` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_010` @ `004496c0`** — owned override

```c
void __thiscall C3DYokCargo::vfunc_01_010(C3DYokCargo *this)

{
  C3DAI::vfunc_01_010((C3DAI *)this);
  this[0x235].vftable = (undefined *)0x0;
  this[0x236].vftable = (undefined *)0x0;
  this[0x255].vftable = (undefined *)0x0;
  return;
}
```

**`vfunc_01_265` @ `00449bb0`** — owned override

```c
void __thiscall C3DYokCargo::vfunc_01_265(C3DYokCargo *this)

{
  int iVar1;
  
  iVar1 = FUN_0045fea0(s_SCENE_004ed220);
  C3DAnimated::vfunc_01_265((C3DAnimated *)this);
  if (*(int *)(DAT_00509948 + 0x490) != 0x4c455635) {
    (**(code **)(this[-0x30].vftable + 0x110))(1);
    return;
  }
  if (0x1e9 < iVar1) {
    (**(code **)(this[-0x30].vftable + 0x110))(1);
    return;
  }
  (**(code **)(this[-0x30].vftable + 0x110))(0);
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DYokCargo` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
