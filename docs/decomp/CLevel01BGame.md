# CLevel01BGame

## Identity

| Item | Value |
|---|---|
| RTTI name | `CLevel01BGame` |
| Base chain | `CJimmyGame -> CGameType -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004c385c, 004c386c, 004c3cbc, 004c3cd0` |
| Ctor(s) | inherits the `CJimmyGame` construction path; installs the `CLevel01BGame` vftables and binds this level's class id |
| Dtor(s) | inherited `CJimmyGame` deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`CLevel01BGame` is the **Level 1B game-mode controller** — a leaf subclass of [`CJimmyGame`](./CJimmyGame.md). It walks 374 vtable slots with **0 owned methods** — pure inheritance, so its entire game lifecycle (InitGame/UnInitGame/update, the active player tick, mission timers, `SoundEffects.omt`) is inherited from `CJimmyGame` / [`CGameType`](./CGameType.md). What distinguishes it is the level it binds: **`level1b.gam`**.

## Level Data (`level1b.gam`)

| Item | Value |
|---|---|
| `.gam` file | `level1b.gam` |
| Placed objects | 129 |
| Distinct object FourCC types | 24 |
| Spawn (from `NewGame.tsk`) | (470.6, 609.2, -87.8) |

Object FourCC composition (top types):

| FourCC | Count |
|---|---:|
| `3AIT` | 20 |
| `3PIC` | 19 |
| `3PAT` | 16 |
| `3CAM` | 13 |
| `3MCA` | 12 |
| `3SOU` | 9 |
| `3LIO` | 7 |
| `3NEU` | 5 |
| `3OMT` | 4 |
| `STRT` | 4 |
| `3ANI` | 2 |
| `3ARR` | 2 |

### Task entity-state table (`NewGame.tsk`)

This level is the new-game entry point; its `CTaskList` seeds the mission entity states below (see [`CTaskList.md`](./CTaskList.md)).

| Tag | State |
|---|---:|
| `MOM` | 0 |
| `LIBBY` | 0 |
| `BENNY` | 0 |
| `SCENE` | 30 |
| `DINO` | 0 |
| `CLONE` | 0 |
| `KITTY1` | 0 |
| `KITTY2` | 0 |
| `KITTY3` | 0 |
| `HYDRANT1` | 0 |
| `HYDRANT2` | 0 |
| `REACTOR` | 0 |

Full per-object placement, properties, and tuning for this level live in `docs/gam_schema.md` (cross-level FourCC schema) and `docs/tsk_data/all_entities.json` (this level's survey). The controller itself does not re-implement object behavior — it loads the `.gam` and lets each placed object's class run.

## Vtable Methods

No owned vtable methods. All 374 slots resolve to inherited `CJimmyGame` / `CGameType` / `CGameObject` behavior.

## Confidence

Confidence: Medium-Low

Validation: Ghidra `DumpClass.java CLevel01BGame` (slots=374, owned_methods=0); vftables/base chain from `docs/decomp_ledger.csv`; level object survey from `tools/build_level_entity_index.py`. Behavior is inherited from `CJimmyGame` (separately specced and the substantive parent). Not runtime-validated.

Open questions:
- Pin this class's constructor and the exact level class-id immediate it registers.
- Confirm the `.gam`/`.tsk` binding is established in the ctor vs. an inherited load slot.

## Notes

- Generated by `tools/gen_level_game_specs.py` from the Ghidra dump + `all_entities.json`. Leaf of `CJimmyGame`; see that spec for the shared controller behavior.
