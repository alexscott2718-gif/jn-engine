# C3DPhoneBooth

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DPhoneBooth` |
| Base chain | `C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004adf5c, 004adf6c, 004ae3bc, 004ae3f8, 004ae40c` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | scalar deleting destructor `vfunc_03_002` at `004356e0` |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DPhoneBooth` is a placeable **world props terrain** object (family `world_props_terrain`, wave 7). It walks the class vtable with 4 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

No own `.gam` properties registered in `InitObject` (inherits its parent's property set, or is created at runtime rather than placed). See `docs/gam_schema.md` for any inherited properties.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `00435780` | InitObject (property + asset registration) | inherited init + class registration |
| `vfunc_01_010` | `00435760` | post-init / per-frame logic | runs inherited per-frame logic then this class's update step |
| `vfunc_01_016` | `00435800` | owned override | see decompiled body |
| `vfunc_03_002` | `004356e0` | scalar deleting destructor | destroys the `OMediaClassStreamer` subobject and frees the allocation |

### Decompiled owned methods

**`vfunc_01_007` @ `00435780`** — InitObject (property + asset registration)

```c
void __thiscall C3DPhoneBooth::vfunc_01_007(C3DPhoneBooth *this)

{
  undefined *puVar1;
  undefined4 uVar2;
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DAnimated::vfunc_01_007((C3DAnimated *)this);
  puVar1 = (undefined *)FUN_0046a910(s_phone_omt_004f0314);
  this[0x12a].vftable = puVar1;
  *(undefined1 *)&this[0x15d].vftable = 0;
  puVar1 = this[-0x30].vftable;
  uVar2 = FUN_00477ba0(this[0x12a].vftable,0);
  (**(code **)(puVar1 + 0xac))(uVar2);
  uVar2 = (**(code **)(this[-0x30].vftable + 0xb0))();
  FUN_00477550(uVar2);
  (**(code **)(this[-0x30].vftable + 0xcc))();
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

**`vfunc_01_010` @ `00435760`** — post-init / per-frame logic

```c
void __thiscall C3DPhoneBooth::vfunc_01_010(C3DPhoneBooth *this)

{
  CLocalGameObject::vfunc_00_010((CLocalGameObject *)this);
  (**(code **)(this->vftable + 0x110))(0x42200000);
  return;
}
```

**`vfunc_01_016` @ `00435800`** — owned override

```c
void __thiscall C3DPhoneBooth::vfunc_01_016(C3DPhoneBooth *this)

{
  char cVar1;
  int *in_stack_00000004;
  
  CGameObject::vfunc_00_016((CGameObject *)this);
  cVar1 = (**(code **)(*in_stack_00000004 + 0x18))(s_C3DJIMMY_004ecb20);
  if (cVar1 != '\0') {
    if (in_stack_00000004 != (int *)0x0) {
      (**(code **)(in_stack_00000004[-0x30] + 0x1d4))();
      return;
    }
    (**(code **)(iRam00000000 + 0x1d4))();
  }
  return;
}
```

**`vfunc_03_002` @ `004356e0`** — scalar deleting destructor

```c
C3DPhoneBooth * __thiscall C3DPhoneBooth::vfunc_03_002(C3DPhoneBooth *this)

{
  byte in_stack_00000004;
  
  FUN_00435710();
  OMediaClassStreamer::~OMediaClassStreamer((OMediaClassStreamer *)(this + 0x1a6));
  if ((in_stack_00000004 & 1) != 0) {
    FUN_004789a0(this + -0xc);
  }
  return this + -0xc;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DPhoneBooth` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
