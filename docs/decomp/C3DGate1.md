# C3DGate1

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DGate1` |
| Base chain | `C3DYokDoor -> C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `0049e278, 0049e288, 0049e6d8, 0049e714, 0049e728` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DGate1` is a placeable **mechanisms moving parts** object (family `mechanisms_moving_parts`, wave 6). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

Offsets are from the primary class pointer; types are the `.gam` serialization type ids (`1=string 2=flag4 3=float 4=raw4 6=int`).

| Offset | Type | Property | Source |
|---:|---|---|---|
| `0x17f` | int | `ItemClosed` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x181` | string | `Next` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x1b5` | float | `DoorSpeed` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x1b6` | float | `OpenTime` | `InitObject` registrar (`vftable+0x3fc`) |

See `docs/gam_schema.md` for the per-FourCC value ranges/samples across all 35 levels (the field map, constants, and object wiring are data-driven from there).

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `0041be00` | InitObject (property + asset registration) | registers 4 `.gam` properties (`ItemClosed`, `Next`, `DoorSpeed`, `OpenTime`) |
| `vfunc_01_266` | `0041bef0` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_007` @ `0041be00`** — InitObject (property + asset registration)

```c
void __thiscall C3DGate1::vfunc_01_007(C3DGate1 *this)

{
  undefined *puVar1;
  undefined4 uVar2;
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DAnimated::vfunc_01_007((C3DAnimated *)this);
  (**(code **)(this->vftable + 0x3fc))(s_ItemClosed_004ee2e0,this + 0x17f,6,0);
  (**(code **)(this->vftable + 0x3fc))(&DAT_004edd18,this + 0x181,1,0);
  (**(code **)(this->vftable + 0x3fc))(s_DoorSpeed_004ee2d4,this + 0x1b5,3,0);
  (**(code **)(this->vftable + 0x3fc))(s_OpenTime_004ee2c8,this + 0x1b6,3,0);
  puVar1 = (undefined *)FUN_0046a910(s_objects_omt_004ecca4);
  this[0x12a].vftable = puVar1;
  *(undefined1 *)&this[0x15d].vftable = 0;
  puVar1 = this[-0x30].vftable;
  uVar2 = FUN_00477ba0(this[0x12a].vftable,1);
  (**(code **)(puVar1 + 0xac))(uVar2);
  uVar2 = (**(code **)(this[-0x30].vftable + 0xb0))();
  FUN_00477550(uVar2);
  (**(code **)(this[-0x30].vftable + 0xcc))();
  (**(code **)(this->vftable + 0x2c))();
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

**`vfunc_01_266` @ `0041bef0`** — owned override

```c
void __thiscall C3DGate1::vfunc_01_266(C3DGate1 *this)

{
  int iVar1;
  
  iVar1 = FUN_0045fea0(s_SCENE_004ed220);
  if (0x149 < iVar1) {
    if (this[0x180].vftable != (undefined *)0x0) {
      FUN_00458980(0xffffffff,0x96,0);
    }
    C3DYokDoor::vfunc_01_266((C3DYokDoor *)this);
    return;
  }
  FUN_00458980(0xffffffff,0x98,0);
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DGate1` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
