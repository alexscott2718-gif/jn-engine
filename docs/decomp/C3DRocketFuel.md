# C3DRocketFuel

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DRocketFuel` |
| FourCC | `3FUE` |
| Base chain | `C3DSpriteType -> C3DSprite -> OMediaCanvasElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004b2048, 004b2058, 004b24a8, 004b24bc` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DRocketFuel` is a placeable **pickups items** object (family `pickups_items`, wave 5). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

`InitObject` registers no properties of its own -- the set this class receives is inherited.

That is not the same as there being no data. The corpus places `3FUE` **7 times** and
`docs/gam_schema.md` harvests **14 properties** from those instances, with names,
types and value ranges; see its `3FUE` section. Which of them a parent registers
rather than this class is not recoverable from the schema -- its check marks record
whether `gam_loader.c` maps a property onto a named `Entity` field, not who declared it.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_016` | `0043d700` | owned override | see decompiled body |
| `vfunc_01_264` | `0043d7b0` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_016` @ `0043d700`** — owned override

Interpreted: type-checks an object via `IsA("C3DJIMMY")`; fires an exit/deactivate action (slot `0x58`).

```c
void __thiscall C3DRocketFuel::vfunc_01_016(C3DRocketFuel *this)

{
  char cVar1;
  int iVar2;
  int *in_stack_00000004;
  
  CGameObject::vfunc_00_016((CGameObject *)this);
  cVar1 = (**(code **)(*in_stack_00000004 + 0x18))(s_C3DJIMMY_004ecb20);
  if (cVar1 != '\0') {
    (**(code **)(this[-0x32].vftable + 0x58))(1);
    iVar2 = FUN_0045fea0(s_SCENE_004ed220);
    FUN_004038c0(2,0x1a,1);
    FUN_004061d0(0x1a,0);
    FUN_0042adc0(100);
    if (iVar2 == 0x1d6) {
      FUN_0045f990(s_SCENE_004ed220,0x1e0);
      FUN_004061c0(0x19,0xffffffff);
      iVar2 = FUN_004061b0(0x19);
      if (iVar2 < 1) {
        FUN_00403910(2,0x19);
      }
    }
    (**(code **)(this->vftable + 0xd0))(0);
  }
  return;
}
```

**`vfunc_01_264` @ `0043d7b0`** — owned override

Interpreted: fires an exit/deactivate action (slot `0x58`).

```c
void __thiscall C3DRocketFuel::vfunc_01_264(C3DRocketFuel *this)

{
  if (this[-0x16].vftable != (undefined *)0x0) {
    this[-0x16].vftable = (undefined *)0x1;
    (**(code **)(this[-0x32].vftable + 0x58))(0);
  }
  (**(code **)(this->vftable + 0x3ac))();
  (**(code **)(this->vftable + 0x214))(0);
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Validation

No field map of this class's own to cross-check -- `InitObject` registers none. The
inherited set is not empty: 14 properties across 7 instances of `3FUE` are harvested
in `docs/gam_schema.md`.

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DRocketFuel` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
