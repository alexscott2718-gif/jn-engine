# C3DHydrant

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DHydrant` |
| FourCC | `3HYD` |
| Base chain | `C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004a24f8, 004a2508, 004a2958, 004a2994, 004a29a8` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DHydrant` is a placeable **world props terrain** object (family `world_props_terrain`, wave 7). It walks the class vtable with 1 owned method; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

`InitObject` registers no properties of its own -- the set this class receives is inherited.

That is not the same as there being no data. The corpus places `3HYD` **2 times** and
`docs/gam_schema.md` harvests **19 properties** from those instances, with names,
types and value ranges; see its `3HYD` section. Which of them a parent registers
rather than this class is not recoverable from the schema -- its check marks record
whether `gam_loader.c` maps a property onto a named `Entity` field, not who declared it.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `00420f70` | InitObject (property + asset registration) | loads `waterhydrant.ase`, `whydrant0000.png`, `whydrant0001.png`, `whydrant0002.png`, `whydrant0003.png`, `DEFAULT` |

### Decompiled owned methods

**`vfunc_01_007` @ `00420f70`** — InitObject (property + asset registration)

```c
void __thiscall C3DHydrant::vfunc_01_007(C3DHydrant *this)

{
  C3DHydrant *pCVar1;
  undefined2 extraout_var;
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DAnimated::vfunc_01_007((C3DAnimated *)this);
  pCVar1 = this + -0x30;
  (**(code **)(this[-0x30].vftable + 0x108))();
  (**(code **)(pCVar1->vftable + 0xd8))(s_HIDEFAULT_004ed8e4,s_waterhydrant_ase_004eeea0);
  (**(code **)(pCVar1->vftable + 0xf0))(s_whydrant0000_png_004eee8c,0);
  (**(code **)(pCVar1->vftable + 0xfc))(0,6,1);
  (**(code **)(pCVar1->vftable + 0xf0))(s_whydrant0001_png_004eee78,1);
  (**(code **)(pCVar1->vftable + 0xfc))(1,6,1);
  (**(code **)(pCVar1->vftable + 0xf0))(s_whydrant0002_png_004eee64,2);
  (**(code **)(pCVar1->vftable + 0xfc))(2,6,1);
  (**(code **)(pCVar1->vftable + 0xf0))(s_whydrant0003_png_004eee50,3);
  (**(code **)(pCVar1->vftable + 0xfc))(3,6,1);
  this[0x17f].vftable = (undefined *)0x0;
  (**(code **)(pCVar1->vftable + 0xf4))
            (this[0x12f].vftable,CONCAT22(extraout_var,*(undefined2 *)&this[0x17f].vftable));
  (**(code **)(this->vftable + 0x110))(0x41a00000);
  (**(code **)(pCVar1->vftable + 0xe0))(s_DEFAULT_004ee39c,1);
  (**(code **)(pCVar1->vftable + 0x114))(0x3e99999a);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

## Assets

| Kind | Name | Present in `assets/` | Notes |
|---|---|---|---|
| ASE/anim | `waterhydrant.ase` | ✓ `waterhydrant.ASE` | anim tag `HIDEFAULT` |
| PNG texture | `whydrant0000.png` | ✓ `whydrant0000.png` |  |
| PNG texture | `whydrant0001.png` | ✓ `whydrant0001.png` |  |
| PNG texture | `whydrant0002.png` | ✓ `whydrant0002.png` |  |
| PNG texture | `whydrant0003.png` | ✓ `whydrant0003.png` |  |
| default anim | `DEFAULT` | n/a | flag 1 |

## Validation

No field map of this class's own to cross-check -- `InitObject` registers none. The
inherited set is not empty: 19 properties across 2 instances of `3HYD` are harvested
in `docs/gam_schema.md`.

## Confidence

Confidence: Low-Medium

Validation: Ghidra `DumpClass.java C3DHydrant` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
