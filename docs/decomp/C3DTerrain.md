# C3DTerrain

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DTerrain` |
| FourCC | `3TER` |
| Base chain | `C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004d4cbc, 004d4ccc, 004d511c, 004d5158, 004d516c` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DTerrain` is a placeable **world props terrain** object (family `world_props_terrain`, wave 7). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

No own `.gam` properties registered in `InitObject` (inherits its parent's property set, or is created at runtime rather than placed). See `docs/gam_schema.md` for any inherited properties.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `00464ac0` | InitObject (property + asset registration) | inherited init + class registration |
| `vfunc_01_011` | `00464b00` | reset / reinit | see decompiled body |

### Decompiled owned methods

**`vfunc_01_007` @ `00464ac0`** — InitObject (property + asset registration)

```c
void __thiscall C3DTerrain::vfunc_01_007(C3DTerrain *this)

{
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DObject::vfunc_01_007((C3DObject *)this);
  (**(code **)(this[-0x30].vftable + 0x34))(DAT_00509a3c);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

**`vfunc_01_011` @ `00464b00`** — reset / reinit

```c
void __thiscall C3DTerrain::vfunc_01_011(C3DTerrain *this)

{
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitPhysics___004f4bec);
  FUN_00479140(this[0x105].vftable);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Validation

FourCC `3TER` has no rows in the 35-level `.gam` corpus (`docs/gam_schema.md`) — this object type is not placed in any shipped level, so there is no `.gam` data to cross-check against.

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DTerrain` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
