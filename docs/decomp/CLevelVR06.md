# CLevelVR06

## Identity

| Item | Value |
|---|---|
| RTTI name | `CLevelVR06` |
| Base chain | `CJimmyGame -> CGameType -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004cf07c, 004cf08c, 004cf4dc, 004cf4f0` |
| Ctor(s) | inherits the `CJimmyGame` construction path; installs the `CLevelVR06` vftables and binds this level's class id |
| Dtor(s) | scalar deleting destructor `vfunc_02_002` at `00457160` (destroys the `OMediaClassStreamer` at `this+0x163`) |
| Ledger row | `docs/decomp_ledger.csv` |

`CLevelVR06` is the **VR challenge level 06 game-mode controller** — a leaf subclass of [`CJimmyGame`](./CJimmyGame.md). It walks 374 vtable slots with **1 owned method** (a per-class destructor only), so its entire game lifecycle (InitGame/UnInitGame/update, the active player tick, mission timers, `SoundEffects.omt`) is inherited from `CJimmyGame` / [`CGameType`](./CGameType.md). What distinguishes it is the level it binds: **`VR06.gam`**.

## Level Data (`VR06.gam`)

| Item | Value |
|---|---|
| `.gam` file | `VR06.gam` |
| Placed objects | 14 |
| Distinct object FourCC types | 10 |

Object FourCC composition (top types):

| FourCC | Count |
|---|---:|
| `3PIC` | 5 |
| `3ARR` | 1 |
| `3JIM` | 1 |
| `3MCA` | 1 |
| `3PAT` | 1 |
| `3PHO` | 1 |
| `3ROC` | 1 |
| `3TRO` | 1 |
| `LOAD` | 1 |
| `STRT` | 1 |

Full per-object placement, properties, and tuning for this level live in `docs/gam_schema.md` (cross-level FourCC schema) and `docs/tsk_data/all_entities.json` (this level's survey). The controller itself does not re-implement object behavior — it loads the `.gam` and lets each placed object's class run.

## Vtable Methods

| Slot | Address | Name | Behavior | Status |
|---|---|---|---|---|
| `vfunc_02_002` | `00457160` | Scalar | scalar deleting destructor | trivial |

## Confidence

Confidence: Medium-Low

Validation: Ghidra `DumpClass.java CLevelVR06` (slots=374, owned_methods=1); vftables/base chain from `docs/decomp_ledger.csv`; level object survey from `tools/build_level_entity_index.py`. Behavior is inherited from `CJimmyGame` (separately specced and the substantive parent). Not runtime-validated.

Open questions:
- Pin this class's constructor and the exact level class-id immediate it registers.
- Confirm the `.gam`/`.tsk` binding is established in the ctor vs. an inherited load slot.

## Notes

- Generated by `tools/gen_level_game_specs.py` from the Ghidra dump + `all_entities.json`. Leaf of `CJimmyGame`; see that spec for the shared controller behavior.
