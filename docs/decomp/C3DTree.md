# C3DTree

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DTree` |
| FourCC | `3TRE` |
| Base chain | `C3DSpriteType -> C3DSprite -> OMediaCanvasElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004bc7c4, 004bc7d4, 004bcc24, 004bcc38` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | scalar deleting destructor `vfunc_02_002` at `00446d90` |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DTree` is a placeable **world props terrain** object (family `world_props_terrain`, wave 7). It walks the class vtable with 1 owned method; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

`InitObject` registers no properties of its own -- the set this class receives is inherited.

That is not the same as there being no data. The corpus places `3TRE` **169 times** and
`docs/gam_schema.md` harvests **14 properties** from those instances, with names,
types and value ranges; see its `3TRE` section. Which of them a parent registers
rather than this class is not recoverable from the schema -- its check marks record
whether `gam_loader.c` maps a property onto a named `Entity` field, not who declared it.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_02_002` | `00446d90` | scalar deleting destructor | destroys the `OMediaClassStreamer` subobject and frees the allocation |

### Decompiled owned methods

**`vfunc_02_002` @ `00446d90`** — scalar deleting destructor

```c
C3DTree * __thiscall C3DTree::vfunc_02_002(C3DTree *this)

{
  byte in_stack_00000004;
  
  FUN_00446dc0();
  OMediaClassStreamer::~OMediaClassStreamer((OMediaClassStreamer *)(this + 0x16f));
  if ((in_stack_00000004 & 1) != 0) {
    FUN_004789a0(this + -0xc);
  }
  return this + -0xc;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Validation

No field map of this class's own to cross-check -- `InitObject` registers none. The
inherited set is not empty: 14 properties across 169 instances of `3TRE` are harvested
in `docs/gam_schema.md`.

## Confidence

Confidence: Low-Medium

Validation: Ghidra `DumpClass.java C3DTree` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
