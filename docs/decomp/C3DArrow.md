# C3DArrow

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DArrow` |
| Base chain | `C3DSpriteType -> C3DSprite -> OMediaCanvasElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `00490940, 00490950, 00490da0, 00490db4` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DArrow` is a placeable **effects triggers nav cameras sound** object (family `effects_triggers_nav_cameras_sound`, wave 8). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

Offsets are from the primary class pointer; types are the `.gam` serialization type ids (`1=string 2=flag4 3=float 4=raw4 6=int`).

| Offset | Type | Property | Source |
|---:|---|---|---|
| `0x149` | string | `RequiredTask` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x15d` | int | `RequiredLevel` | `InitObject` registrar (`vftable+0x3fc`) |
| `0x15e` | int | `ExactLevel` | `InitObject` registrar (`vftable+0x3fc`) |

See `docs/gam_schema.md` for the per-FourCC value ranges/samples across all 35 levels (the field map, constants, and object wiring are data-driven from there).

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `0040f4f0` | InitObject (property + asset registration) | registers 3 `.gam` properties (`RequiredTask`, `RequiredLevel`, `ExactLevel`) |
| `vfunc_01_264` | `00419aa0` | reset / reinit | see decompiled body |

### Decompiled owned methods

**`vfunc_01_007` @ `0040f4f0`** — InitObject (property + asset registration)

```c
void __thiscall C3DArrow::vfunc_01_007(C3DArrow *this)

{
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DSprite::vfunc_01_007((C3DSprite *)this);
  (**(code **)(this->vftable + 0x3fc))(s_RequiredTask_004ed608,this + 0x149,1,0);
  (**(code **)(this->vftable + 0x3fc))(s_RequiredLevel_004ed294,this + 0x15d,6,0);
  (**(code **)(this->vftable + 0x3fc))(s_ExactLevel_004ed288,this + 0x15e,6,0);
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

**`vfunc_01_264` @ `00419aa0`** — reset / reinit

```c
void __thiscall C3DArrow::vfunc_01_264(C3DArrow *this)

{
  undefined4 uVar1;
  
  CGameObject::vfunc_00_013((CGameObject *)this);
  uVar1 = FUN_0045fea0(this + 0x10c);
  FUN_0045f990(this + 0x10c,uVar1);
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DArrow` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
