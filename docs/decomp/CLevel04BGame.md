# CLevel04BGame

## Identity

| Item | Value |
|---|---|
| RTTI name | `CLevel04BGame` |
| Base chain | `CJimmyGame -> CGameType -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `004c975c, 004c976c, 004c9bbc, 004c9bd0` |
| Ctor(s) | inherits the `CJimmyGame` construction path; installs the `CLevel04BGame` vftables and binds this level's class id |
| Dtor(s) | inherited `CJimmyGame` deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`CLevel04BGame` is the **Level 4B game-mode controller** — a leaf subclass of [`CJimmyGame`](./CJimmyGame.md). It walks 374 vtable slots with **2 owned methods**, so its entire game lifecycle (InitGame/UnInitGame/update, the active player tick, mission timers, `SoundEffects.omt`) is inherited from `CJimmyGame` / [`CGameType`](./CGameType.md). What distinguishes it is the level it binds: **`level4b.gam`**.

### Level-specific overrides

| Slot | Address | Role | Referenced literals |
|---|---|---|---|
| `vfunc_01_259` | `004538b0` | reset/reload override | `POWER` |
| `vfunc_01_265` | `00453690` | level-progress gate override | — |

## Level Data (`level4b.gam`)

| Item | Value |
|---|---|
| `.gam` file | `level4b.gam` |
| Placed objects | 92 |
| Distinct object FourCC types | 21 |

Object FourCC composition (top types):

| FourCC | Count |
|---|---:|
| `3STE` | 17 |
| `3NEU` | 15 |
| `3FUE` | 7 |
| `3SWI` | 7 |
| `3RED` | 6 |
| `3SOU` | 6 |
| `3FAN` | 5 |
| `3CAM` | 4 |
| `3PAT` | 4 |
| `3DOR` | 3 |
| `3PEN` | 3 |
| `STRT` | 3 |

Full per-object placement, properties, and tuning for this level live in `docs/gam_schema.md` (cross-level FourCC schema) and `docs/tsk_data/all_entities.json` (this level's survey). The controller itself does not re-implement object behavior — it loads the `.gam` and lets each placed object's class run.

## Vtable Methods

| Slot | Address | Name | Behavior | Status |
|---|---|---|---|---|
| `vfunc_01_259` | `004538b0` | Reset/Reload | reset/reload override (refs `POWER`) | non-trivial |
| `vfunc_01_265` | `00453690` | Level-Progress | level-progress gate override | non-trivial |

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java CLevel04BGame` (slots=374, owned_methods=2); vftables/base chain from `docs/decomp_ledger.csv`; level object survey from `tools/build_level_entity_index.py`. Behavior is inherited from `CJimmyGame` (separately specced and the substantive parent). Not runtime-validated.

Open questions:
- Pin this class's constructor and the exact level class-id immediate it registers.
- Decompile the level-specific override(s) above to recover the level mission logic (not just the referenced literals).
- Confirm the `.gam`/`.tsk` binding is established in the ctor vs. an inherited load slot.

## Notes

- Generated by `tools/gen_level_game_specs.py` from the Ghidra dump + `all_entities.json`. Leaf of `CJimmyGame`; see that spec for the shared controller behavior.
