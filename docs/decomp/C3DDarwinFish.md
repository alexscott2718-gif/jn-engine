# C3DDarwinFish

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DDarwinFish` |
| FourCC | `3FIS` |
| Base chain | `C3DEnemy -> C3DPickupType -> C3DAI -> C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `00498150, 00498160, 004985b0, 004985ec, 00498600` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DDarwinFish` is a placeable **creatures one off set dressing** object (family `creatures_one_off_set_dressing`, wave 9). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Runtime-confirmed behavior (game owner, 2026-06-23)

This class is a **shrink-ray target**, not inert set-dressing. The shrink ray (`C3DShrinkRay`/`3SHR`) shrinks it: it plays `HISHRINK` (`darwinshrink.ASE`, registered by `vfunc_04_067`), scales down, and becomes a small **moving pickup** the player can collect — which is exactly why the chain carries `C3DPickupType` (the global pickup-state table `DAT_004f8438`). The shrink-on-contact → `HISHRINK` → moving-pickup transition body is not decompiled (`C3DShrinkRay` only animates the ray; `3SHR` has zero `.gam` placements), so the native port records this truth and defers the active mechanic. See `docs/decomp/C3DShrinkRay.md` and `src/game/behaviors/behavior_creature.c`.

## Field Map (registered `.gam` properties)

`InitObject` registers no properties of its own -- the set this class receives is inherited.

That is not the same as there being no data. The corpus places `3FIS` **12 times** and
`docs/gam_schema.md` harvests **27 properties** from those instances, with names,
types and value ranges; see its `3FIS` section. Which of them a parent registers
rather than this class is not recoverable from the schema -- its check marks record
whether `gam_loader.c` maps a property onto a named `Entity` field, not who declared it.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_007` | `004173c0` | InitObject (property + asset registration) | inherited init + class registration |
| `vfunc_04_067` | `00416d00` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_007` @ `004173c0`** — InitObject (property + asset registration)

```c
void __thiscall C3DDarwinFish::vfunc_01_007(C3DDarwinFish *this)

{
  CGameObject *this_00;
  
  (**(code **)(this->vftable + 0x3f8))(this,s_InitObject___004eca2c);
  C3DPickupType::vfunc_01_007((C3DPickupType *)this);
  (**(code **)(this[-0x30].vftable + 0x108))();
  CGameObject::vfunc_00_013(this_00);
  return;
}
```

**`vfunc_04_067` @ `00416d00`** — owned override

```c
void __thiscall C3DDarwinFish::vfunc_04_067(C3DDarwinFish *this)

{
  undefined *puVar1;
  
  if (*(char *)((int)&this[0x18d].vftable + 1) == '\0') {
    puVar1 = this->vftable;
    *(undefined1 *)((int)&this[0x18d].vftable + 1) = 1;
    (**(code **)(puVar1 + 0xd8))(s_HIWALK_004ed808,s_darwinwalk_ase_004ee17c);
    (**(code **)(this->vftable + 0xd8))(s_HISHRINK_004ee15c,s_darwinshrink_ASE_004ee168);
    (**(code **)(this->vftable + 0xd8))(s_HISTOP_004ec9f4,s_darwinstop_ase_004ee14c);
    (**(code **)(this->vftable + 0xf0))(s_darwin_png_004ee140,0);
    (**(code **)(this->vftable + 0xf4))(this[0x15f].vftable,0);
    (**(code **)(this->vftable + 0xe0))(&DAT_004ed040,1);
  }
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Validation

No field map of this class's own to cross-check -- `InitObject` registers none. The
inherited set is not empty: 27 properties across 12 instances of `3FIS` are harvested
in `docs/gam_schema.md`.

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DDarwinFish` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
