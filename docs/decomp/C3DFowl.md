# C3DFowl

## Identity

| Item | Value |
|---|---|
| RTTI name | `C3DFowl` |
| FourCC | `3FOW` |
| Base chain | `C3DFriends -> C3DAI -> C3DAnimated -> C3DObject -> OMedia3DMorphAnim -> OMedia3DShapeElement -> OMediaElement -> OMediaWorldPosition -> OMediaWorldAngle -> OMediaElementContainer -> OMediaDBObject -> OMediaClassStreamer -> OMediaListener -> OMediaMessagePort -> OMediaAnim -> CLocalGameObject -> CGameObject` |
| Vftable(s) | `0049d5f0, 0049d600, 0049da50, 0049da8c, 0049daa0` |
| Ctor(s) | factory/constructor installs the vftables and registers the class id (see `docs/_gam_classids.tsv`) |
| Dtor(s) | inherited deleting destructor (none owned) |
| Ledger row | `docs/decomp_ledger.csv` |

`C3DFowl` is a placeable **creatures one off set dressing** object (family `creatures_one_off_set_dressing`, wave 9). It walks the class vtable with 2 owned methods; its `.gam`-driven parameters and assets are registered in `InitObject` and listed below.

## Field Map (registered `.gam` properties)

`InitObject` registers no properties of its own -- the set this class receives is inherited.

That is not the same as there being no data. The corpus places `3FOW` **4 times** and
`docs/gam_schema.md` harvests **35 properties** from those instances, with names,
types and value ranges; see its `3FOW` section. Which of them a parent registers
rather than this class is not recoverable from the schema -- its check marks record
whether `gam_loader.c` maps a property onto a named `Entity` field, not who declared it.

## Vtable Methods (owned)

| Slot | Address | Role | Behavior |
|---|---|---|---|
| `vfunc_01_265` | `0041b380` | owned override | see decompiled body |
| `vfunc_04_067` | `0041b2a0` | owned override | see decompiled body |

### Decompiled owned methods

**`vfunc_01_265` @ `0041b380`** — owned override

```c
void __thiscall C3DFowl::vfunc_01_265(C3DFowl *this)

{
  int iVar1;
  int iVar2;
  
  iVar2 = FUN_0045fea0(s_SCENE_004ed220);
  C3DAnimated::vfunc_01_265((C3DAnimated *)this);
  iVar1 = *(int *)(DAT_00509948 + 0x490);
  if (iVar1 == 0x4c455634) {
    if ((0x121 < iVar2) && (iVar2 < 0x136)) {
LAB_0041b3fd:
      (**(code **)(this[-0x30].vftable + 0x110))(1);
      return;
    }
  }
  else if (iVar1 == 0x4c563441) {
    if (499 < iVar2) {
      (**(code **)(this[-0x30].vftable + 0x110))(1);
      return;
    }
  }
  else {
    if (iVar1 != 0x4c563443) {
      return;
    }
    if ((0x1cb < iVar2) && (iVar2 < 0x1d6)) goto LAB_0041b3fd;
  }
  (**(code **)(this[-0x30].vftable + 0x110))(0);
  return;
}
```

**`vfunc_04_067` @ `0041b2a0`** — owned override

```c
void __thiscall C3DFowl::vfunc_04_067(C3DFowl *this)

{
  undefined *puVar1;
  
  if (*(char *)((int)&this[0x18d].vftable + 1) == '\0') {
    puVar1 = this->vftable;
    *(undefined1 *)((int)&this[0x18d].vftable + 1) = 1;
    (**(code **)(puVar1 + 0xd8))(s_HIWALK_004ed808,s_fowlwalk_ase_004ee74c);
    (**(code **)(this->vftable + 0xd8))(s_HITALK_004ed7f0,s_fowltalk_ase_004ee73c);
    (**(code **)(this->vftable + 0xd8))(s_HITELE_004edc04,s_fowlteleport_ase_004ee728);
    (**(code **)(this->vftable + 0xd8))(s_HICHEER_004edbec,s_fowlcheer_ase_004ee718);
    (**(code **)(this->vftable + 0xd8))(s_HISTOP_004ec9f4,s_fowlstop_ase_004ee708);
    (**(code **)(this->vftable + 0xf0))(s_fowl_png_004ee6fc,0);
    (**(code **)(this->vftable + 0xf4))(this[0x15f].vftable,0);
    (**(code **)(this->vftable + 0xe0))(&DAT_004ed040,1);
    (**(code **)(this[0x30].vftable + 0x110))(0x43160000);
    (**(code **)(this[0x30].vftable + 0x100))(0x3f000000);
  }
  return;
}
```

## Assets

No direct ASE/PNG/anim references in `InitObject` (inherited visual path or runtime-assigned).

## Validation

No field map of this class's own to cross-check -- `InitObject` registers none. The
inherited set is not empty: 35 properties across 4 instances of `3FOW` are harvested
in `docs/gam_schema.md`.

## Confidence

Confidence: Medium

Validation: Ghidra `DumpClass.java C3DFowl` (owned methods decompiled); `.gam` properties and assets resolved from the `InitObject` registrar calls with strings read directly from `Neutron.exe`. `.gam` value ranges cross-referenced via `docs/gam_schema.md`. Behavioral prose is derived from the decompiled bodies above; not runtime-validated.

Open questions:
- Confirm the gameplay semantics of the per-frame/owned override method(s) beyond the decompiled control flow.
- Pin the constructor address and class-id immediate (FourCC).

## Notes

- Generated by `tools/gen_placeable_specs.py` from the Ghidra dump + PE string resolution. Decompiled bodies are included verbatim as primary evidence.
