# Linked-Parity Scoreboard

Generated 2026-08-20 by `tools/check_linkage_certificates.py`.

Source of truth: `docs/linkage_certificates.csv`. A `linked` row is counted
here only after its oracle ran green in this pass (see
`docs/linked_parity_plan.md` for the Linkage Certificate L1-L5 contract).

- **linked (oracle-verified):** 16
- **linked-blocked (returns to native-port):** 15

## linked

| class | aspect | domain | oracle | proof |
|---|---|---|---|---|
| `CTaskList` | tsk-deserialization | progression / objectives | `tools/linkage_oracles/CTaskList.py` | PASS CTaskList: task_parse_file == tsk_parser.py byte-exact on 5 synthesized .tsk streams; NewGame table matches CTaskList.md (SCENE=30) |
| `CLoadLevel` | gam-deserialization | progression / objectives | `tools/linkage_oracles/CLoadLevel.py` | PASS CLoadLevel: gam_load == gam_parser.parse_gam byte-exact on 35 shipped .gam files (3299 objects, 97 LOAD rows -- all 9 docs/decomp/CLoadLevel.md properties) |
| `CTaskList` | set-task-state | progression / objectives | `tools/linkage_oracles/CTaskList_set_task_state.py` | PASS CTaskList/set-task-state: task_set_entity_state == FUN_0045f990's table-write loop (case-insensitive match, write-existing-only, never append) across 4 write sequences over the documented NewGame table |
| `C3DCutSceneCamera` | 3cam-camera-math | camera / cutscene | `tools/linkage_oracles/C3DCutSceneCamera.py` | PASS C3DCutSceneCamera/3cam-camera-math: cutscene_3cam_dist byte-exact on 544 (row x t) samples across all 136 shipped 3CAM rows; cutscene_3cam_place's CameraType==2 static-branch precedence over ViewFromCamera byte-exact on all 16 real CT==2 rows; full orbit/dolly placement matches recovered transform_local on 351 real/synthetic cases; ViewFromCamera/CameraType distributions match docs/decomp/C3DCutSceneCamera.md |
| `C3DMultiCutSceneCamera` | 3mca-offset-table | camera / cutscene | `tools/linkage_oracles/C3DMultiCutSceneCamera.py` | PASS C3DMultiCutSceneCamera/3mca-offset-table: cutscene_mca_local_offset byte-exact on 5436 (entry x t) samples across 906 real CameraTypeN entries in the documented 0..4 range (all 114 shipped 3MCA rows x up to 8 steps); full world placement and look point match recovered transform_local on 1053 real/synthetic cases; 6 out-of-table entries excluded per the doc's scope note |
| `C3DCutSceneCamera` | 3cam-full-placement | camera / cutscene | `tools/linkage_oracles/C3DCutSceneCamera.py` | PASS C3DCutSceneCamera/3cam-camera-math: cutscene_3cam_dist byte-exact on 544 (row x t) samples across all 136 shipped 3CAM rows; cutscene_3cam_place's CameraType==2 static-branch precedence over ViewFromCamera byte-exact on all 16 real CT==2 rows; full orbit/dolly placement matches recovered transform_local on 351 real/synthetic cases; ViewFromCamera/CameraType distributions match docs/decomp/C3DCutSceneCamera.md |
| `C3DMultiCutSceneCamera` | 3mca-full-placement | camera / cutscene | `tools/linkage_oracles/C3DMultiCutSceneCamera.py` | PASS C3DMultiCutSceneCamera/3mca-offset-table: cutscene_mca_local_offset byte-exact on 5436 (entry x t) samples across 906 real CameraTypeN entries in the documented 0..4 range (all 114 shipped 3MCA rows x up to 8 steps); full world placement and look point match recovered transform_local on 1053 real/synthetic cases; 6 out-of-table entries excluded per the doc's scope note |
| `CJimmyGame` | initgame-seed | progression / objectives | `tools/linkage_oracles/CJimmyGame.py` | PASS CJimmyGame/initgame-seed: game_flow_init_game reproduces the decompiled CJimmyGame::InitGame (0044d3d0) mission seed exactly (lives=5, mission_value=100), confirmed against a zero pre-seed baseline and idempotent re-seed |
| `C3DStartPoint` | spawn | progression / objectives | `tools/linkage_oracles/C3DStartPoint.py` | PASS C3DStartPoint/spawn: place_player reproduces PlacePlayer (00442740) STRT resolution (case-insensitive), teleport-to-transform, and MusicDatabase/MusicIndex selection byte-exact across all 100 shipped STRT rows in 35 levels (270 requests incl. case-varied, @default and miss cases) |
| `C3DPatrolPoint` | on-arrive | AI / pathing | `tools/linkage_oracles/C3DPatrolPoint.py` | PASS C3DPatrolPoint/on-arrive: behavior_ai_find_patrol_point + gam_prop_f reproduce NextPatrolPoint resolution and WaitTime byte-exact across all 742 shipped 3PAT waypoints in 35 levels (581 edges resolve to a real neighbor) |
| `C3DAITrigger` | dispatch-graph | triggers / story sequencing | `tools/linkage_oracles/C3DAITrigger.py` | PASS C3DAITrigger/dispatch-graph: aitrig_activate byte/value-exact on 9 target-mutation scenarios (hide/show/AINewPos/AINewRotY/AIPatrol/ToggleObject/NextTrigger/trigger_count) and aitrig_apply_story_progress byte-exact on 7 SCENE story-progress scenarios (gate hit, gate miss, and the KITTY special case) against the recovered ActivateAITrigger/ApplyAITriggerStoryProgress bodies |
| `C3DPickupItem` | collection | progression / objectives | `tools/linkage_oracles/C3DPickupItem.py` | PASS C3DPickupItem/collection: item_on_trigger reproduces HandlePickupCollection's order and effects -- gate before the collected-state check, ActivateObject/ToggleObject state dispatch before the PIC_NUMBER and score award, NextTrigger after it, sound last -- plus CheckRequiredPicAndConsume's consume-or-refuse arithmetic and PostLoadPickupItem's InitallyActive gate, across all 383 shipped 3PIC rows in 35 levels |
| `CMainMenu` | level-routing-table | menus / UI flow | `tools/linkage_oracles/CMainMenu.py` | PASS CMainMenu/level-routing-table: menu.c routes the executable's .rdata:004ec71c table exactly (NewGame->level1b task route + 8 VR levels in menu order VR01,VR03,VR02,VR08,VR06,VR07,VR05,VR04), item count pinned at 10 via selection wrap, driven through the real menu_open/menu_input/menu_take_confirm path |
| `CGameType` | initgame-camera-record-seed | progression / objectives | `tools/linkage_oracles/CGameType.py` | PASS CGameType/initgame-camera-record-seed: camera_record_init_game matches CGameType::InitGame 00474a10 position writes exactly two exact seeds; adjacent angles/mode preserved |
| `C3DTriggerType` | nexttrigger-camera-retarget | triggers / story sequencing | `tools/linkage_oracles/C3DTriggerType.py` | PASS C3DTriggerType/nexttrigger-camera-retarget: camera_record_retarget matches the recovered slot-242 record write (00447a70) on 516 synthetic cases covering all octants of the 14-bit angle space (incl. wrap-negative shorts) x mixed real-magnitude target positions; fixed offset (20,-20,-100) and native Z mirror verified; no authored data table feeds this function (runtime-state-only inputs) |
| `C3DPlayer` | walking-camera-record | camera / player-follow | `tools/linkage_oracles/C3DPlayer_walkcam.py` | PASS C3DPlayer/walking-camera-record: camera_record_walkcam_write matches the recovered UpdateWalkingCameraB record write (00439900) on 294 synthetic cases covering both yaw-quantization paths (plain 00472980 ftol+mask vs the 0043a5d0 negative/360 folds), nonzero roll, turn leads, 14-bit record-angle octants (incl. wrap-negative shorts), shipped-level position magnitudes, the ProbePlayerRayBlend 75%-blend/1.5x path, and zero-direction degenerates; axis scales (1.2, 2.0, 1.2), the OMedia angles() snap-from-snapshot write, and the native Z mirror verified (max angle deviation 0 table units); no authored data table feeds this function (runtime-state-only inputs) |

## linked-blocked (missing evidence or an unported dependency)

| class | aspect | domain | why it cannot be linked here |
|---|---|---|---|
| `C3DPlayer` | free-roam-feel | player movement | Target 3 recovered the movement/action L1 bodies for UpdateGroundMoveA (00437c40), UpdateJumpFallMove (00437f90), UpdateWalkingCameraA/B (00438bc0/00439900), SetPlayerAnimationState (0043aff0), and the remaining C3DPlayer raw helper list; see docs/decomp/evidence/c3dplayer_movement_target3.md. The recovered bodies pin the real walk-speed accumulator/clamps, turn-rate clamps, jump/fall phase state, camera smoothing, scratch/action animation randomization, fence/ladder setup, load handoff, and ray/probe helpers. The row remains linked-blocked because L2 fails by design: native behavior_player.c is the approved tank-turn movement, and movement_base.c is a tuned approximation, not a 1:1 port. Certifying player movement requires a product/native-port decision to replace the approved movement with the recovered state machine, then an input-trace oracle plus mutation test. 2026-07-02 follow-up: the walking-camera half of UpdateWalkingCameraA/B -- the DAT_00509a50 record write -- is now interpreted (docs/decomp/evidence/walking_camera_record_write.md), ported (camera_record_walkcam_write), and certified as the separate C3DPlayer/walking-camera-record row; the movement half stays blocked as described. |
| `C3DGoddard` | texture-uv | animation / actor pose | Goddard texture/UV correctness is an art-fidelity issue with no headless oracle; needs capture/original comparison on native-port. |
| `C3DCindy` | location-pathing | AI / pathing | Cindy's correct location/patrol state is unresolved and requires original-game or capture evidence; do not guess. Returns to native-port. |
| `C3DSoundEffect` | by-ear-mix | triggers / story sequencing | Audio timbre/mix/by-ear correctness needs desktop/noVNC listening; the dispatch/trigger LOGIC is separately linkable. Returns to native-port. |
| `C3DCarl` | vehicle-rider-pose | vehicles / special movement | Carl's vehicle insertion pose/offset needs decomp proof or capture evidence; the vehicle integrator MATH is separately linkable. Returns to native-port. |
| `C3DAnimated` | ase-deserialization | animation / actor pose | ase_loader.c parses this project's own OMT->ASE exporter output (PROJECT_HISTORY.md Era 2 -- OMT->ASE exporter), not a Neutron.exe binary format -- no decompiled body exists to certify against (L1 unsatisfiable). The original binary mesh format is OMT/3DSP, already decoded and settled (docs/omt_3dsp_format.md); diffing ase_loader.c against tools/ase_parser.py would only prove two readers of this project's own re-export format agree with each other, which the plan excludes as self-comparing. Mesh/pose/texture visual correctness is the real faithfulness question and needs by-eye comparison. Returns to native-port. |
| `C3DAnimated` | event-animation-dispatch | animation / actor pose | Target 7 function-defined and dumped the C3DAnimated event-to-animation cluster (docs/decomp/evidence/c3danimated_target7.md): UpdateAnimated (0040e050), SetAnim3DByName (0040dd90), CreateAnim3DRecord (0040d4a0), InitAnim3DDatabase (0040e270), and raw helper targets 0040e1f0/0040d9e0/0040da30/0040dab0/0040db10/0040df80/0040e3e0/0040d350. L1 pins the original OMedia animation-record dispatcher and last-frame path: base C3DAnimated calls vtable-4 slot 65 when the selected clip reaches its cached last frame, base slot 65 is shared no-op/thunk 00472970, and C3DPlayer consumes the event by overriding the slot at 0043a900 (OnPlayerAnimEnded). OMT 2.5 source resolved the embedded OMediaAnim layout, confirming play_loop as the completion gate, float-millisecond frame walking, same-record reselect semantics, and the no-latch re-fire behavior. The native module in src/game/animated_dispatch.c/h is covered by tools/linkage_oracles/C3DAnimated_dispatch.py. 2026-07-03 runtime wiring now reaches it from behavior_animated_update_base, cutscene actor selection, player_anim entity records, FENCE/LADDER/SPLAT/HIT AnimEnded-to-STOP completion, and existing projectile/Yokian/Tesla damage paths that enter HIT/SPLAT; tools/linkage_oracles/C3DAnimated_runtime.py mutation-tests those wiring points. The row still stays linked-blocked because live record frame counts/timing come from native/exported ASE AseModel metadata rather than recovered original A3dm sequence/import data, and faithful FENCE entry still needs original collision material/probe data and restore-transform behavior. |
| `C3DCheckPoint` | progress | progression / objectives | Native vt_checkpoint (behavior_checkpoint.c) is a deliberate simplification, not a port of the decompiled UpdateCheckPoint (00414410): its own comment says it matches the original's checkpoint-progression FEEL via last-touched-wins respawn relocation, with no FINISHLINE check, race-timer gate (DAT_004eefc8), finish call (FUN_004073b0), or HUD time draw. Same shape as CJimmyGame's win-bridge exclusion: a working native behavior the project chose not to make 1:1 with the recovered body, so there is no fidelity claim to certify. Porting the actual FINISHLINE/race-timer mechanism is real behavior-porting work, out of scope for a linkage-certification pass. Returns to native-port. |
| `CTrigger` | enter-exit-latch | triggers / story sequencing | Target 5 resolved the family's identities and opened full L1 (docs/decomp/evidence/triggertype_trigger_target5.md). TRIG's RTTI is pinned: the TRIG factory FUN_0047dcf0 is the CTrigger constructor (installs the four CTrigger vftables, default tag CTRIGGER @004f6c38, class id 'TRIG' @0047de03), so TRIG = CTrigger; gam_schema.md updated. The recovered mechanism is complete: watched-list sphere latch with debounced enter (slot 21/0x54) and exit (slot 22/0x58) dispatch; trigger_radius is an owned unregistered float at base+0x5e8 (ctor default 10.0f) distinct from LightRange (+0x4b4), so the one shipped TRIG row's LightRange 1e+04 never reaches it; the only static caller of UpdateTrigger is the newly boundary-repaired CTriggerTimer (TRIT, zero shipped instances) and RegisterTarget (0047df30) has no static code caller at all, so no watcher is statically registered for the one placed TRIG. L2 still fails by design: native TRIG (behavior_trig.c vt_trig) is a deliberate one-shot log stub fired by the native engine's own AABB-overlap dispatch (src/engine/physics.c), which re-fires every overlapping frame and has no exit event, watched list, or sphere radius. No fidelity claim exists to certify; porting the watched-list/latched-edge mechanism is native-port behavior work. C3DTrigger/3TRI's inert native cascade is unchanged. Returns to native-port. |
| `CGameType` | pause-help-update-gates | menus / UI flow | Fresh 9a2b908 evidence identifies PauseGame trace helper 00475a00, separate DAT_005099a9 writer 00475a30, UpdateGameType 00475a70, and ToggleHelp 00475ce0, but the committed L1 explicitly leaves DAT_005099a9 semantics, the update predicate producer, and help show/hide target slots 0x13c/0x140 unresolved. The workstation has no original executable/Ghidra project to deepen those bodies. Porting pause/input locks or help presentation from the current keyboard menu would guess behavior; recover those bodies first, then obtain original/capture evidence for the help layout. |
| `CLoadLevel` | activate-load | progression / objectives | ActivateLoad (00458370) is decoded -- hide the portal via slot 0xd8 and hand the +0x17a request block (LevelName/StartPoint/fade) to global game-object slot 0x100. Target 2 also recovered the missing contact/gate body at 00457ec0: Jimmy-only contact, RequiredTask lookup via FUN_0045fea0, RequiredLevel minimum, optional ExactLevel, special RETURN path, normal LevelName/StartPoint handoff, optional SoundIndex, and optional FadeType/FadeTime. The native path (behavior_load.c load_on_trigger + behavior_base.c gate helpers) is still a functional bridge, not a transcription: it forwards LevelName/StartPoint via gamestate_request_level_swap, adds a native fire-once latch, applies the gate at spawn/update as well as trigger time, has no RETURN branch, does not port the Jimmy handoff slots, and drops the original sound/fade request semantics. An oracle around the current native path would certify a different design, not the recovered body. Returns to native-port: port the recovered 00457ec0/00458370 semantics 1:1, then write the oracle. |
| `C2DInGameMenu` | hud-draw | menus / UI flow | DrawHud (00406690) pins four counter positions/formats and the RestartLevel.tsk + level1f.gam death calls, but 9a2b908 still has eight raw owned helpers, unresolved counter producers/death predicate, and no recovered RestartLevel.tsk. The capture-backed hud_layout_generated.h proves 33 visual quads in frame 8881 only; it cannot infer those gameplay producers/conditions. Native hud.c has two different counters and game_flow does not load RestartLevel.tsk. Recover 00402b40..00407490 and the task file before replacing the current HUD; doing so from one frame would fabricate parity. |
| `CMainMenu` | menu-manager-screen-graph | menus / UI flow | Target 4 recovers the 29-record LoadMyMenu/displayMenu state mechanics, item-state writes, counter pulses, and save/task calls around DAT_004f8164. The immutable 9a2b908 evidence does not include the DAT_004f8164 table contents (sprite IDs/positions/screen graph), Menu_ItemRolloverState_00403890 failed recovery, activation audio is not body-backed, and level-controller handoff remains open. Native menu.c is only the separately linked routing-table keyboard list. Reconstructing the original layout from it would be a hand approximation; recover the executable-backed records/helpers first. |
| `CMenuElement` | update-item-logic | menus / UI flow | UpdateItemLogic (0045e650) is structurally recovered: query slots 0x38 then 0x14; on zero dispatch this+0xc through parent_or_input[0x23]+0xc, show cursor, then run inherited update. Its owning DAT_004f8164 canvas records are absent, and 9a2b908 explicitly leaves hit-state polarity and target-slot meaning conservative; no activation-sound caller is recovered. Native keyboard selection is not the same object protocol. Recover the CMainMenu canvas owner/target semantics and sound caller before a runtime port/oracle. |
| `C3DJimmy` | gadget-mode-dispatch | inventory / gadgets | Target 6 function-defined and dumped the Jimmy frame/controller/setup/menu-lock/helper cluster (docs/decomp/evidence/c3djimmy_target6.md): UpdateJimmyFrame (00424600), UpdateJimmyActiveController (00426030), JimmySetupOrReset (00423610), InitJimmyRuntimeHandles (00425db0), JimmyEnterActionMenuLock (00425ef0), level snapshot helpers (0042af00/0042af50), remaining vtable-4 helpers, and ctor 00401430. L1 now pins the old raw gadget surface: 0xa18 is a code-created C2DInGameMenu controller (object size 0x51c; registration string 0x4ef05c = C2DInGameMenu), 0x95c is code-spawned C3DGoddard, 0x970 is hidden C3DJeep, DAT_004f83d4 is the special-level oxygen/countdown, DAT_004eefc8 is the race timer, and DAT_004eefd0 is the secondary warning/restart countdown. L2 fails by design: native behavior_player.c is the approved tank-turn/simple tool-use player path, game_flow.c only has the simplified lives/restart bridge, and native has no C2DInGameMenu gadget controller protocol, Goddard/Jeep companion spawn, or oxygen/race countdown port. An oracle over the current native inventory/tool path would certify a different design. Returns to native-port: port the recovered controller/timer/mode dispatch first, then write an oracle plus mutation test. |
